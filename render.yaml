services:
  - type: web
    name: claimsure-api
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn ClaimSure_backend.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: claimsure-db
          property: connectionString

databases:
  - name: claimsure-db
    databaseName: claimsure_db
    user: claimsure_user
    plan: free
